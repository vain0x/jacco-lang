/* never */ void abort();

void print_i32(int value_1);

int main();

int main() {
    // match_result_1 is killed.
    switch (42) {
        default: {
            int x_1 = 42;
            // call_result_1 is killed.
            print_i32(x_1);
            goto match_next_1;
        }
    }

match_next_1:;
    return 0;
}
