pub fn main() -> i32 {
    if (1) {}
    if (2) {}

    if (3) {
        let body3 = 30;
    } else if (4) {
        let alt31 = 31;
    } else {
        let alt32 = 32;
    }

    let unit = ();
    let group_expr = (1);

    let a = 1;
    let p = &a;
    let pp1 = &(&a);
    let pp2 = &p;
    let p1d = *p;
    let pp1dd = **pp1;
    let pp2dd = **pp2;

    // exit code
    0
}

fn fn_with_params(x: i32, y: i32, _unit: ()) -> i32 {
    let s = x + y;
    let t = x - y;
    s * t
}

fn fn_with_params_user() {
    fn_with_params(1, 2, ());
}
